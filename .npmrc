# Demos link the workspace root; without this pnpm hoists inconsistently for
# the framework peer deps and the demos resolve two copies of react/vue.
shamefully-hoist=false
strict-peer-dependencies=false
